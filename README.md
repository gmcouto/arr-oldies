# Arr-Oldies

CLI tool and auditing engine that connects to multiple Radarr and Sonarr instances to inventory all downloaded media files, correlate them with precise download and import event timestamps from the *arr History API, and list media files sorted by oldest downloads/imports.
